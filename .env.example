# Copy to .env and fill in only what you need.
# Groot is offline-first: leave everything below unset until you
# reach the phase that needs it.

# Phase 6 (Escape Hatch) — cloud model API key, opt-in per task only.
# GROOT_CLOUD_API_KEY=
